{% extends 'layout.html.twig' %}

{% block page_title %}Créer un évènement{% endblock %}

{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-12 ma-25-top">
                <div class="pa-20 bg-white">
                    <p>Créer un nouvel event :</p>
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <input type="submit" value="Créer">
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}